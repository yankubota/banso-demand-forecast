# 需要予測ダッシュボード（仮称） 仕様書（spec.md v1.0）

| 項目 | 内容 |
|---|---|
| ドキュメント名 | 需要予測ダッシュボード（仮称） 仕様書 v1.0 |
| ステータス | **Single Source of Truth（唯一の正）** — 本書と実装が乖離した場合、本書を正とする |
| 作成日 | 2026-07-03（JST） |
| 作成者 | 株式会社Banso 久保田（協力：Claude） |
| 本番URL | 未定（Vercel／yankubotas-projects 配下を想定） |
| リポジトリ | 未定（リポジトリ直下に `spec.md` として配置する） |

---

## 0. DDD運用ルール（本書の使い方）

1. 本書＝唯一の正（SSOT）。機能の追加・変更・削除は、必ず本書の該当節を先に更新する。
2. 順序の固定：`仕様変更 → 人間（久保田）が承認 → AIが実装 → 動作確認 → 変更履歴に記録`。コードから先に変更しない。
3. 非破壊マイグレーション：機能削除は「①書き込み停止 → ②安定確認後（最低2週間）に物理削除」の2段階。実施前にバックアップを案内。
4. 変更履歴の義務：§9 に「版数・日付・変更内容・対応ID」を必ず追記する。
5. AIへの指示：本書を渡し「本仕様に基づいて実装せよ。記載のない仕様の推測実装を禁じる」と指示する。

## 1. 目的（何のために作るか）

**一言ピッチ**：POS・レジのCSVを放り込むだけで、「明日つくる数・発注する数」が朝イチでわかる無料ダッシュボード。

- **Pain**：中小企業（食品製造小売・飲食・小売等）の発注・仕込み量判断は熟練者の勘に依存し、廃棄ロスと欠品が同時に発生している。既存の需要予測SaaSは月額数万円〜で過大。
- **Gain**：ブラウザ完結・無料・データは端末外に出ない設計で、①発注業務時間の削減、②廃棄率と欠品の同時削減、③予測根拠の見える化（脱属人化）を提供する。
- **事業文脈**：令和8年度 茨城県センター事業の「現場実装ダッシュボード」（企画提案書 追加提案④）の汎用版。**本アプリはBanso独自開発の資産であり、センター納品成果物とはコードベース・文書を分離する**（§8 R-1）。

## 2. 対象ユーザー（誰が使うか）

| 優先 | ユーザー | 利用文脈 |
|---|---|---|
| 一次 | 中小企業の発注・仕込み担当者（店長・工場長・仕入担当） | 毎朝、当日の生産数・翌日以降の発注量を決める |
| 二次 | 経営者 | 週次で精度・廃棄・欠品の推移を確認し、投資判断する |
| 三次 | 支援者（認定サポーター・ITC・商工会議所講師） | 講座・伴走支援のデモ／導入支援ツールとして使う |

## 2.5 用語集（ユビキタス言語）

| 用語 | 定義 | コード上の識別子 |
|---|---|---|
| 商品（SKU） | 予測対象の最小単位。商品名または商品コードで一意 | `product` / `productId` |
| 販売実績 | 日付×SKUの販売数量。**本アプリの唯一の学習データ（正）** | `sales` / `qty` |
| 予測値 | 手法が算出した将来日別の需要見込み（導出値） | `forecast` |
| 予測手法 | MA（移動平均）／DOW（曜日プロファイル）／HW（Holt-Winters加法） | `METHOD_MA` / `METHOD_DOW` / `METHOD_HW` |
| バックテスト | 直近ホールドアウト期間を隠して各手法の誤差を測る検証 | `backtest` |
| 自動選択 | SKUごとにバックテスト誤差最小の手法を採用する仕組み | `autoSelect` |
| MAPE | 平均絶対誤差率（%）。実績0の日は計算から除外 | `mape` |
| WAPE | 加重絶対誤差率（%）＝Σ|誤差|÷Σ実績。ゼロ売上が多いSKUの主指標 | `wape` |
| 安全在庫 | 予測誤差に備える上乗せ量＝z×σ×√(対象日数) | `safetyStock` |
| サービス水準 | 欠品を許容しない確率。z値に対応（90/95/99%） | `serviceLevel` / `zValue` |
| リードタイム | 発注から納品までの日数 | `leadTimeDays` |
| 発注サイクル | 発注間隔の日数 | `orderCycleDays` |
| 発注推奨量 | ceil(対象期間予測合計＋安全在庫−現在庫)。負は0 | `recommendedQty` |
| 現在庫 | 発注画面でユーザーが手入力する当日在庫（P0は手入力のみ） | `currentStock` |
| 学習期間 | 予測に使う過去実績の範囲 | `trainRange` |

## 3. 機能の優先順位（必須 / 任意）

### P0：必須（本版のスコープ）

| ID | 機能 | 仕様概要 | 状態 |
|---|---|---|---|
| ADD-01 | CSV取込ウィザード | 3ステップ（ファイル→列マッピング→確認）。UTF-8／Shift_JIS自動判定。日付・商品・数量の3列で成立 | ⏳ |
| ADD-02 | 予測エンジン＋自動選択 | MA／DOW／HWの3手法をSKU別バックテストし、誤差最小の手法を自動採用 | ⏳ |
| ADD-03 | ダッシュボード | 「今日の推奨」カード／予測vs実績チャート（SVG内製）／精度パネル（MAPE・WAPE・手法名） | ⏳ |
| ADD-04 | 発注推奨＋帳票出力 | 現在庫を入力→SKU別推奨量を算出→発注書CSVをダウンロード | ⏳ |
| ADD-05 | JSONエクスポート／インポート | 全データのバックアップ・復元（インポート前に自動バックアップ） | ⏳ |

### P1 / P2：任意（優先順・本版では着手しない）

| ID | 機能 | 仕様概要 |
|---|---|---|
| ADD-06 | 天候・祝日補正 | 気象データ・祝日マスタを補正係数として予測に反映 |
| ADD-07 | 業種テンプレート5種 | 菓子製造小売／ベーカリー／青果／飲食／小売の初期設定プリセット |
| ADD-08 | PWA対応 | 店舗タブレットへのホーム画面追加・オフライン閲覧 |
| ADD-09 | 在庫CSV取込 | 現在庫の一括取込（手入力の置き換え） |
| ADD-10 | POSプリセット | スマレジ等の主要POSエクスポート形式の列マッピング保存 |
| ADD-11 | Web Worker化 | 大規模データ時の予測計算をバックグラウンド化 |
| ADD-12 | BigQuery／GA4連携・n8nアラート（P2） | L2層。外部先行指標の統合と通知 |

### P3：スコープ外（着手禁止）

| ID | 機能 | 概要 |
|---|---|---|
| FUT-01 | AIチャット解説 | Claude API による「なぜこの予測か」の対話応答（L3層） |
| FUT-02 | マルチテナントSaaS化 | アカウント・課金・サーバ側予測 |
| FUT-03 | クラウド同期 | 複数端末間のデータ同期 |

## 4. 完了条件（どうなれば完成か：DoD）

- [ ] D-1：P0全件（ADD-01〜05）が本番環境（Vercel）で動作確認済み
- [ ] D-2：同梱サンプルCSVで「取込→予測→発注推奨→発注書CSV出力」の一連が**3分以内**に完了する
- [ ] D-3：SKUごとに採用手法名とMAPE／WAPEが画面で確認できる（根拠の見える化）
- [ ] D-4：JSONエクスポート→別ブラウザでインポート→データが完全復元される
- [ ] D-5：合成データ 500SKU×2年で、予測の全再計算が**3秒以内**・保存サイズ**4MB以内**
- [ ] D-6：本書 §9 変更履歴が最新実装と一致している（ドリフトゼロ）

## 5. 機能仕様

### ADD-01 CSV取込ウィザード

- **Step1 ファイル選択**：`.csv` をドラッグ&ドロップ／選択。文字コードはUTF-8（BOM有無とも）を優先試行し、置換文字（U+FFFD）検出時に `TextDecoder('shift_jis')` で再デコード（外部ライブラリ不使用）。
- **Step2 列マッピング**：先頭10行をプレビューし、必須3列（日付／商品／数量）＋任意1列（店舗）をプルダウンで対応付け。日付形式は `YYYY-MM-DD`・`YYYY/M/D`・`YYYYMMDD` を自動判定。数量は整数化（小数は四捨五入）。
- **Step3 確認・取込**：件数・期間・SKU数・エラー行数（日付不正・数量非数値）を表示して実行。**同日同SKUの複数行は合算**。既存データと期間が重複する日は「上書き」を既定とし、実行前に対象日数を明示。
- 商品マスタは取込時に商品名から自動生成（既存名と完全一致なら同一SKU扱い）。
- エラー行はスキップし、行番号つきでダウンロード可能なログを提示（データを黙って捨てない）。

### ADD-02 予測エンジン＋自動選択

対象：翌日から `leadTimeDays + orderCycleDays` 日先までの日別予測。全ロジックは `logic/forecast.js`・`logic/backtest.js` に純粋関数で実装する。

| 手法 | 定義 | 適用条件 |
|---|---|---|
| MA | 直近N日の単純平均（既定 N=28。実績日数<Nは全期間平均） | 実績7日以上 |
| DOW | 予測日と同曜日の直近K週平均（既定 K=4） | 実績14日以上 |
| HW | Holt-Winters加法・周期m=7。α,β,γ∈{0.1, 0.3, 0.5} の27通りをグリッド探索し、バックテスト誤差最小の組を採用。初期値：レベル＝先頭7日平均、季節指数＝曜日別平均−全体平均 | 実績28日以上 |

- **バックテスト**：ホールドアウト＝直近14日（実績42日未満のSKUは直近7日）。残り期間で学習し、ホールドアウトのMAPE・WAPEを算出。
- **自動選択**：適用条件を満たす手法のうちMAPE最小を採用。同率はWAPE→MA（単純側）の順でタイブレーク。実績7日未満のSKUは「予測不可（データ不足）」バッジを表示し、推奨対象から除外。
- **精度指標**：MAPEは実績0の日を除外して算出。実績0の日が対象期間の50%超のSKUは、画面の主指標をWAPEに切り替える（ゼロ除算・過大評価対策）。
- 予測値は0未満に丸めない負値が出た場合0にクリップし、表示は整数（ceil）。

### ADD-03 ダッシュボード

- **今日の推奨カード**：当日予測数の上位N件（既定 N=10、設定変更可）を「商品名／推奨生産・準備数／採用手法」で表示。
- **予測vs実績チャート**：直近30日の折れ線（実績）＋帯（予測）。全SKU合計／SKU選択の切替。SVG内製（外部チャートライブラリ禁止）。
- **精度パネル**：全体WAPE、SKU別ワースト5（MAPE降順）を表示し、精度が悪いSKUを改善対象として可視化。
- 初回起動時（データなし）は、サンプルデータ読込ボタンと取込ウィザード導線を表示（空画面を作らない）。

### ADD-04 発注推奨＋帳票出力

- 入力：SKU別の `currentStock`（手入力・その場保存）。未入力SKUは在庫0とみなす旨を明示。
- 計算：`recommendedQty = max(0, ceil(Σforecast(翌日〜L+C日) + z×σ×√(L+C) − currentStock))`
  - σ＝バックテスト残差の標準偏差（SKU別・日次）。z＝サービス水準90/95/99%に対応する1.28/1.65/2.33（既定95%）。
- 出力：発注書CSV（UTF-8 BOM付き・Excel互換）。列＝商品名／商品コード／推奨量／予測合計／安全在庫／現在庫／採用手法。ファイル名 `order_YYYYMMDD.csv`。
- 一覧は推奨量降順。0件のSKUは折りたたみ表示。

### ADD-05 JSONエクスポート／インポート

- エクスポート：`{ schemaVersion, exportedAt, products, sales, settings }` を1ファイルで出力。ファイル名 `df_backup_YYYYMMDD_HHmm.json`。
- インポート：`schemaVersion` を検証（未来版は拒否・旧版はマイグレーション）。**実行前に現行データを自動エクスポート**してから置換（Reward Timer ADD-01と同一パターン）。置換内容（SKU数・期間）を確認ダイアログで明示。

## 6. データ仕様

**集約（整合性の単位）と正／導出の区別**：

| 集約 | 正となるデータ | キー | スキーマ概要 |
|---|---|---|---|
| 商品カタログ | ✅ 正 | `df_products` | `[{ id, name, code?, active }]`（idは連番文字列 `p1, p2…`） |
| 販売実績 | ✅ 正 | `df_sales` | 疎な辞書 `{ "YYYY-MM-DD": { productId: qty } }`（売れた分のみ保持） |
| 設定 | ✅ 正 | `df_settings` | `{ serviceLevel, leadTimeDays, orderCycleDays, maWindow, dowWeeks, holdoutDays, topN, currentStock: {productId: qty} }` |
| メタ | ✅ 正 | `df_meta` | `{ schemaVersion: 1 }` |
| 予測・精度キャッシュ | ❌ 導出（破棄可能） | `df_cache_forecast` | 入力ハッシュ＋結果。スキーマ不一致時は自動破棄し再計算 |

- 永続化は `window.storage` を優先し、失敗時に `localStorage` へフォールバック（banso-tech-stack標準ラッパーを `logic/storage.js` に実装。キー接頭辞 `df_`）。
- 予測結果・発注推奨は**常に正データから再計算可能**でなければならない（キャッシュ消失で機能が壊れない）。
- 保存前に総サイズを概算し、4MBを超える場合は警告＋古い期間のアーカイブエクスポートを案内する。

**ディレクトリ構成（banso-tech-stack準拠）**：

```
src/
├── App.jsx / main.jsx
├── state/            # reducer.js（全状態遷移）・context.jsx
├── logic/            # forecast.js / backtest.js / order.js / csv.js / metrics.js / storage.js（純粋関数）
├── constants/        # 既定値・z値表・日付形式
└── components/
    ├── shared/       # KpiCard, Chart(SVG), Wizard, Nav ほか
    ├── import/  dashboard/  order/  settings/
```

## 7. 非機能要件

| 項目 | 要件 |
|---|---|
| 対応環境 | 最新のChrome／Edge／Safari。レスポンシブ（スマホ〜タブレット〜PC） |
| パフォーマンス | 500SKU×2年で全SKU予測再計算3秒以内（超過が判明した場合はADD-11を繰り上げ） |
| セキュリティ | 販売データを外部送信しない（fetchは自ホストのみ）。`dangerouslySetInnerHTML` 禁止。機微データ平文保存につき「共有端末での利用非推奨」を設定画面に常設表示 |
| デザイン | banso-design-system準拠（OKLCHセマンティックトークン・8pxグリッド・WCAG AA）。**Bansoブランドカラー未確定のため仮トークン値を使用**（確定後にプリミティブ層のみ差替え） |
| 技術方針 | 外部ランタイム依存ゼロ（React/Vite以外のnpm依存を追加しない）。JS＋JSDoc、TypeScript不使用 |
| 文言 | UI・コード・本書で用語集（§2.5）の語を統一（DDD②） |

## 8. 想定リスク・制約

| 優先 | リスク | 対策 |
|---|---|---|
| 高 | R-1 知財混同：センター事業成果物（著作権センター帰属）との混同 | 本アプリはBanso独自リポジトリで新規開発。センター納品物のコード・文書を流用しない。事例数値の掲載はセンター許諾後 |
| 高 | R-2 欠品バイアス：実績＝需要とみなすため、欠品日は過小予測になる | P0は制約として明記（UIの精度パネルに注記）。欠品フラグ入力はP1で検討 |
| 中 | R-3 MAPEのゼロ除算・過大評価 | 実績0日を除外＋WAPE併記（§5 ADD-02で仕様化済み） |
| 中 | R-4 データ不足SKUの誤信頼 | 7日未満は予測対象外、28日未満はHW不適用＋「参考値」バッジ |
| 中 | R-5 localStorage容量・消失 | 疎形式保存＋4MB警告＋ADD-05バックアップを初回起動時に案内 |
| 低 | R-6 名称・商標 | 仮称のまま開発（機能に影響なし）。公開前に商標調査 |

**未決事項（本書の次版で確定する）**

| 項目 | 現状 | 決定期限の目安 |
|---|---|---|
| プロダクト名称 | 仮称「需要予測ダッシュボード」。storageキー接頭辞は `df_` で**確定扱い**（名称変更の影響を受けない） | 一般公開前 |
| ブランドカラー | banso-design-systemの仮トークン値を使用 | Bansoブランドカラー確定時 |
| サポーター報酬率ほか事業条件 | 保留（本書のスコープ外。事業側ドキュメントで管理） | — |

## 9. 変更履歴

| 版 | 日付（JST） | 変更内容 | 対応ID |
|---|---|---|---|
| v1.0 | 2026-07-03 | 初版作成。P0スコープ（ADD-01〜05）・アルゴリズム既定値・データ集約・DoDを定義 | ADD-01〜05, FUT-01〜03 |
| v1.0.1 | 2026-07-03 | P0実装 v0.1.0 を納品（仕様変更なし）。仕様が定めない境界の解釈3件を README_P0.md §実装ノートに記録 | ADD-01〜05 |

---

*本書を更新せずにコードを変更することを禁ずる。— Document-Driven Development*
